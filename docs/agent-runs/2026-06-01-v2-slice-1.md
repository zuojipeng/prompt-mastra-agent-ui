# Agent Run · V2 Slice 1 Contract Stabilization

Date: 2026-06-01
Owner: Hermes Orchestrator
Scope: `V2-STABILIZATION-PLAN.md` Slice 1

## Goal

Stabilize the V2 DirectorKit contract so frontend and backend stop drifting.

## Product Agent

Status: PASS

Input:
- Product direction: AI short-film director workspace
- Slice goal: contract stability before broader UI/E2E work

Output:
- Accepted scope: DirectorKit schema, target duration/type constants, focused contract test
- Out of scope: UI state redesign, full browser E2E, deployment changes

Acceptance criteria:
- Backend validates full DirectorKit response structure
- Frontend uses centralized V2 contract types/constants
- Automated tests catch frontend enum drift

## UI Agent

Status: PASS WITH FOLLOW-UP

Input:
- Current `ChatBox` V2 flow
- Existing target duration/type controls

Output:
- UI constants moved out of component into `lib/director-kit-contract.ts`
- Component now consumes centralized constants

Follow-up:
- Slice 2 must produce a full V2 UI state matrix for loading/error/retry/empty/success.

## Engineering Agent

Status: PASS

Backend output:
- Added `src/mastra/schemas/director-kit.ts`
- Replaced shallow required-field parsing with zod schema parsing
- Centralized backend V2 duration/type/platform constants

Frontend output:
- Added `lib/director-kit-contract.ts`
- Re-exported contract types through `lib/api-client.ts`
- Updated `ChatBox` to use contract constants
- Added `__tests__/director-kit-contract.test.ts`

Commits:
- Backend: `b08617c Validate DirectorKit responses with schema`
- Frontend: `51f1f83 Extract DirectorKit frontend contract`

## Code Review Agent

Status: PASS WITH RISK

Findings:
- P0: none
- P1: none
- P2: frontend and backend contract constants are still duplicated across repos, not generated from one package.

Risk:
- Cross-repo enum drift can still happen if only one repo changes.

Recommendation:
- For the next contract change, update both repos in the same run and keep tests as drift alarms.
- Consider a future generated OpenAPI/JSON schema artifact if the API surface grows.

## Test Agent

Status: PASS

Automated checks executed:
- Backend `npm test`
- Frontend `npm test`
- Frontend `npx tsc --noEmit`
- Frontend `npm run lint`
- Frontend `npm run build`
- Frontend `npm run test:smoke`

New automated coverage:
- `__tests__/director-kit-contract.test.ts`

Remediation added:
- `scripts/e2e-director-kit-test.mjs`
- `npm run test:e2e:v2`

V2 live E2E result:
- PASS, 2/2 cases
- Default sandbox run failed with `fetch failed`; approved external-network run passed against the deployed Worker.

## DevOps Agent

Status: NOT RUN

Reason:
- Slice 1 was code and contract stabilization only.

Required before release:
- Worker dry-run
- Pages build config confirmation
- `/api/health` check
- V2 live API E2E against deployed Worker

## Hermes Decision

Slice 1 is acceptable as a contract stabilization slice. The original run was incomplete from a process standpoint because it lacked formal Agent artifacts and V2 E2E automation.

Corrective action:
- This report, the test plan, the test report, and the V2 E2E script were added after review.
- The V2 E2E script was executed and passed against the deployed Worker.
