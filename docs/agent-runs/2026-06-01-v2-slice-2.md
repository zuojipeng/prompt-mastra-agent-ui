# Agent Run · V2 Slice 2 UI State Recovery

Date: 2026-06-01
Owner: Hermes Orchestrator
Scope: `V2-STABILIZATION-PLAN.md` Slice 2

## Goal

Improve V2 UI state recovery so generation loading, errors, edit recovery, keyboard selection, and incomplete results are visible and recoverable.

## Product Agent

Status: PASS

Accepted scope:
- Do not add new product capabilities.
- Focus on making existing V2 flow safer and more recoverable.
- Preserve user input on failure and when returning to edit.

Acceptance criteria:
- Failed generation does not clear input.
- Failed generation exposes retry.
- Loading state is visible beyond the submit button.
- Reconstruct selection is keyboard accessible.
- Result page has an incomplete-data fallback.

## UI Agent

Status: PASS

Output:
- Added `docs/design/v2-ui-state-matrix.md`

Design decisions:
- Error panels use `role="alert"`.
- Loading panel uses `aria-live="polite"`.
- Reconstruct cards behave as radio options.
- Returning to edit preserves current creative and target controls.

## Engineering Agent

Status: PASS

Files changed:
- `app/components/ChatBox.tsx`
- `scripts/smoke-test.mjs`
- `__tests__/chatbox-v2-source.test.ts`

Implementation:
- Added retry action for generation errors.
- Added visible loading panel.
- Disabled target duration/type buttons during V2 loading to avoid target drift.
- Added `radiogroup` / `radio` roles and Enter/Space/Arrow key selection.
- Added incomplete result fallback.
- Added safer array access for partial DirectorKit data.
- Added source-level tests for V2 state implementation.

## Code Review Agent

Status: PASS WITH RESOLVED FINDINGS

Reviewer: Locke

Findings:
- P1 partial data could still throw due direct array dereferences.
- P1 `返回修改` cleared input via `handleResetV2`.
- P2 smoke only checked strings.
- P2 test plan missed validation/partial/disabled-confirm/fallback cases.

Resolution:
- Replaced direct array access with guarded `?? []` in critical sections.
- Added `handleReturnToEdit` to preserve input and target controls.
- Added source-level state tests.
- Expanded test plan and test report.

Reviewer: Halley

Findings:
- P2 target duration/type controls were interactive during loading.
- P2 radio semantics missed arrow-key behavior.
- Remaining browser/manual evidence gap.

Resolution:
- Disabled target duration/type controls during loading.
- Added ArrowUp/ArrowDown/ArrowLeft/ArrowRight support.
- Browser/manual gap recorded in test report as remaining risk.

## Test Agent

Status: PASS WITH MANUAL GAP

Automated checks:
- `npm test`: PASS, 4 test files, 35 tests
- `npx tsc --noEmit`: PASS when run after build completes
- `npm run lint`: PASS, 15 existing warnings, 0 errors
- `npm run build`: PASS
- `npm run test:smoke`: PASS

New coverage:
- `__tests__/chatbox-v2-source.test.ts`
- Smoke checks for loading/retry/incomplete fallback strings

Known test limitation:
- Component render tests are not enabled because current Vitest/Vite config lacks React JSX transform support.
- Source-level tests protect implementation markers but do not replace browser E2E.

## DevOps Agent

Status: NOT RUN

Reason:
- Slice 2 is frontend UI state work only.

Required before release:
- Browser journey check on deployed preview or local dev server.
- Confirm Pages build still uses Node 20+.

## Hermes Decision

Slice 2 is acceptable to commit as UI state recovery work.

Remaining risk:
- Need browser/manual evidence for the full V2 journey before release gate.

