# Test Report · V2 Slice 2 UI State Recovery

Date: 2026-06-01
Owner: Test Agent
Scope: V2 UI loading, error, retry, keyboard selection, incomplete fallback

## Summary

Status: PASS WITH MANUAL GAP

Slice 2 improved recoverability and added automated source/smoke coverage. Local automated checks passed. Full browser journey evidence is still required before release.

## Commands Executed

```bash
npm test
npx tsc --noEmit
npm run lint
npm run build
npm run test:smoke
```

Results:
- `npm test`: PASS, 4 test files, 35 tests
- `npx tsc --noEmit`: PASS when run after `next build` completed
- `npm run lint`: PASS with 15 existing warnings, 0 errors
- `npm run build`: PASS, static export successful, First Load JS about 111 kB
- `npm run test:smoke`: PASS

## New Automated Coverage

Added:
- `__tests__/chatbox-v2-source.test.ts`

Coverage:
- Error recovery markers: `role="alert"`, `重试生成`, retry handler
- Loading markers: `正在生成导演执行包`, `aria-live="polite"`
- Loading target lock: `disabled={v2Loading}`
- Reconstruct accessibility markers: `radiogroup`, `radio`, arrow keys
- Incomplete fallback markers: `执行包内容不完整`, guarded arrays

Updated:
- `scripts/smoke-test.mjs` now checks loading, retry, and incomplete-result strings in the production bundle.

## Review Findings and Resolution

Locke:
- P1 partial DirectorKit arrays could throw.
  - Fixed with guarded `?? []` array access.
- P1 returning to edit cleared input.
  - Fixed with `handleReturnToEdit`.
- P2 smoke was static-only.
  - Mitigated with source-level tests; browser E2E still needed.
- P2 test plan missed validation/partial/fallback cases.
  - Test plan expanded.

Halley:
- P2 target controls remained interactive during loading.
  - Fixed with `disabled={v2Loading}`.
- P2 radio behavior lacked arrow-key navigation.
  - Fixed with ArrowUp/ArrowDown/ArrowLeft/ArrowRight support.

## Execution Notes

`npx tsc --noEmit` failed when run in parallel with `next build` because the build process cleans `.next/types`. It passed when rerun after build completed.

Rule:
- Do not run `next build` and `tsc --noEmit` in parallel for this project.

## Remaining Gaps

- Browser journey evidence is not captured in this report.
- Component-level render tests are not enabled because the current Vitest/Vite setup lacks React JSX transform support.
- Existing lint warnings remain in older code paths.

## Release Confidence

Slice 2 UI state recovery: Medium-high.

Full V2 release confidence remains medium until:
- Browser/manual report validates loading, retry, keyboard selection, and mobile layout.
- Slice 3 E2E journey automation is implemented or explicitly waived.

